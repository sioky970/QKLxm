# 站内信系统功能测试报告

## 测试信息

- **测试时间**：____年____月____日
- **测试人员**：________________
- **测试环境**：
  - Go版本：________________
  - MySQL版本：________________
  - 操作系统：________________
- **测试范围**：站内信系统前后台完整功能

---

## 一、环境准备 ✅

### 1.1 数据库准备
- [ ] MySQL服务已启动
- [ ] 数据库 `bibi2022` 已创建
- [ ] 执行 `站内信系统表结构.sql` 成功
- [ ] 验证3张表已创建：
  - [ ] `messages` 表
  - [ ] `user_messages` 表
  - [ ] `message_templates` 表
- [ ] 验证8条预置模板已插入

**执行结果**：
```
记录执行SQL脚本的输出结果...
```

### 1.2 服务启动
- [ ] Go后端服务启动成功
- [ ] 服务运行端口：8080
- [ ] 无启动错误

**启动日志**：
```
记录服务启动的关键日志...
```

---

## 二、后台管理API测试

### 2.1 消息发送功能

#### 测试用例2.1.1：发送消息给指定用户
- **测试目的**：验证管理员可以发送消息给指定用户
- **测试步骤**：
  1. 使用管理员Token调用 `POST /api/admin/messages/send`
  2. 指定receiver_ids为 [1, 2, 3]
- **预期结果**：返回200，消息创建成功，3条user_messages记录插入
- **实际结果**：□ 通过  □ 失败
- **备注**：
  ```
  记录实际返回的JSON数据...
  ```

#### 测试用例2.1.2：全员发送消息
- **测试目的**：验证全员发送功能
- **测试步骤**：
  1. 调用 `POST /api/admin/messages/send-all`
  2. 发送一条系统通知
- **预期结果**：所有用户都收到消息
- **实际结果**：□ 通过  □ 失败
- **发送用户数量**：______ 人
- **耗时**：______ 秒

#### 测试用例2.1.3：按用户等级发送
- **测试目的**：验证按等级发送功能
- **测试步骤**：调用 `POST /api/admin/messages/send-by-level`
- **预期结果**：指定等级用户收到消息
- **实际结果**：□ 通过  □ 失败

### 2.2 消息管理功能

#### 测试用例2.2.1：获取消息列表
- **测试目的**：验证分页和筛选功能
- **测试步骤**：
  1. 调用 `POST /api/admin/messages/list`
  2. 测试不同筛选条件（type、status、priority）
- **预期结果**：返回符合条件的消息列表
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.2.2：获取消息详情
- **测试目的**：验证详情查询
- **测试步骤**：调用 `GET /api/admin/messages/:id`
- **预期结果**：返回完整消息信息
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.2.3：更新消息
- **测试目的**：验证消息更新功能
- **测试步骤**：
  1. 调用 `PUT /api/admin/messages/:id`
  2. 修改标题和内容
- **预期结果**：消息更新成功
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.2.4：撤回消息
- **测试目的**：验证消息撤回功能
- **测试步骤**：
  1. 调用 `POST /api/admin/messages/withdraw`
  2. 验证用户端是否看不到该消息
- **预期结果**：消息状态改为2（撤回）
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.2.5：删除消息
- **测试目的**：验证消息删除功能（硬删除）
- **测试步骤**：调用 `DELETE /api/admin/messages/:id`
- **预期结果**：消息及所有关联记录被删除
- **实际结果**：□ 通过  □ 失败

### 2.3 统计功能

#### 测试用例2.3.1：获取接收者列表
- **测试目的**：验证查看接收者和已读情况
- **测试步骤**：调用 `GET /api/admin/messages/:id/receivers`
- **预期结果**：返回所有接收者及已读状态
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.3.2：获取单条消息统计
- **测试目的**：验证消息统计准确性
- **测试步骤**：调用 `GET /api/admin/messages/:id/statistics`
- **预期结果**：返回总数、已读数、未读数、已读率
- **实际结果**：□ 通过  □ 失败
- **统计数据**：
  ```
  total_receivers: ______
  read_count: ______
  unread_count: ______
  read_rate: ______%
  ```

#### 测试用例2.3.3：获取全局统计
- **测试目的**：验证全局统计功能
- **测试步骤**：调用 `GET /api/admin/messages/statistics`
- **预期结果**：返回所有消息的统计信息
- **实际结果**：□ 通过  □ 失败

### 2.4 模板管理功能

#### 测试用例2.4.1：获取模板列表
- **测试目的**：验证模板列表查询
- **测试步骤**：调用 `GET /api/admin/message-templates/list`
- **预期结果**：返回8条预置模板
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.4.2：创建消息模板
- **测试目的**：验证模板创建功能
- **测试步骤**：
  1. 调用 `POST /api/admin/message-templates/create`
  2. 创建自定义模板
- **预期结果**：模板创建成功
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.4.3：更新消息模板
- **测试目的**：验证模板更新功能
- **测试步骤**：调用 `PUT /api/admin/message-templates/:id`
- **预期结果**：模板更新成功
- **实际结果**：□ 通过  □ 失败

#### 测试用例2.4.4：删除消息模板
- **测试目的**：验证模板删除功能
- **测试步骤**：调用 `DELETE /api/admin/message-templates/:id`
- **预期结果**：模板删除成功
- **实际结果**：□ 通过  □ 失败

---

## 三、前台用户API测试

### 3.1 消息查询功能

#### 测试用例3.1.1：获取用户消息列表
- **测试目的**：验证用户可以查看自己的消息
- **测试步骤**：
  1. 使用用户Token调用 `GET /api/messages/list`
  2. 测试不同筛选条件
- **预期结果**：只返回该用户的消息
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.1.2：获取消息详情（自动标记已读）
- **测试目的**：验证查看详情时自动标记已读
- **测试步骤**：
  1. 调用 `GET /api/messages/detail?id=xxx`
  2. 查询数据库验证is_read字段
- **预期结果**：is_read变为1，read_time更新
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.1.3：获取未读消息数量
- **测试目的**：验证未读数量统计准确性
- **测试步骤**：调用 `GET /api/messages/unread-count`
- **预期结果**：返回准确的未读数量
- **实际结果**：□ 通过  □ 失败
- **未读数量**：______ 条

### 3.2 消息操作功能

#### 测试用例3.2.1：标记单条消息为已读
- **测试目的**：验证手动标记已读功能
- **测试步骤**：调用 `POST /api/messages/read`
- **预期结果**：消息标记为已读
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.2.2：标记单条消息为未读
- **测试目的**：验证标记未读功能
- **测试步骤**：调用 `POST /api/messages/unread`
- **预期结果**：消息标记为未读
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.2.3：批量标记已读
- **测试目的**：验证批量操作功能
- **测试步骤**：调用 `POST /api/messages/batch-read`
- **预期结果**：指定消息全部标记为已读
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.2.4：全部标记已读
- **测试目的**：验证一键已读功能
- **测试步骤**：调用 `POST /api/messages/read-all`
- **预期结果**：该用户所有消息标记为已读
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.2.5：删除单条消息
- **测试目的**：验证软删除功能
- **测试步骤**：
  1. 调用 `POST /api/messages/delete`
  2. 验证is_deleted字段变为1
  3. 验证其他用户不受影响
- **预期结果**：仅该用户看不到该消息
- **实际结果**：□ 通过  □ 失败

#### 测试用例3.2.6：批量删除消息
- **测试目的**：验证批量删除功能
- **测试步骤**：调用 `POST /api/messages/batch-delete`
- **预期结果**：指定消息全部软删除
- **实际结果**：□ 通过  □ 失败

### 3.3 统计功能

#### 测试用例3.3.1：获取用户消息统计
- **测试目的**：验证统计准确性
- **测试步骤**：调用 `GET /api/messages/statistics`
- **预期结果**：返回总数、未读数、各类型数量
- **实际结果**：□ 通过  □ 失败
- **统计数据**：
  ```
  total_count: ______
  unread_count: ______
  read_count: ______
  system_count: ______
  trade_count: ______
  activity_count: ______
  ```

---

## 四、权限控制测试

### 4.1 用户权限隔离
- **测试目的**：验证用户只能访问自己的消息
- **测试步骤**：
  1. 用户A的Token访问用户B的消息
  2. 用户A尝试删除用户B的消息
- **预期结果**：拒绝访问或返回空数据
- **实际结果**：□ 通过  □ 失败

### 4.2 管理员权限验证
- **测试目的**：验证管理员可以管理所有消息
- **测试步骤**：使用管理员Token访问各类数据
- **预期结果**：可以访问和管理所有消息
- **实际结果**：□ 通过  □ 失败

### 4.3 Token验证
- **测试目的**：验证无Token或错误Token无法访问
- **测试步骤**：
  1. 不携带Token访问
  2. 携带错误Token访问
  3. 携带过期Token访问
- **预期结果**：返回401 Unauthorized
- **实际结果**：□ 通过  □ 失败

---

## 五、性能测试

### 5.1 批量发送性能
- **测试场景**：全员发送（___人）
- **耗时**：______ 秒
- **平均速度**：______ 条/秒
- **是否满足要求**：□ 是  □ 否

### 5.2 查询性能
- **测试场景**：查询消息列表（100条数据）
- **响应时间**：______ ms
- **是否满足要求**：□ 是（<500ms）  □ 否

### 5.3 并发测试
- **工具**：Apache Bench / JMeter
- **并发数**：______
- **请求数**：______
- **成功率**：______%
- **平均响应时间**：______ ms

---

## 六、异常处理测试

### 6.1 参数验证
- [ ] 缺少必填参数时返回400
- [ ] 参数类型错误时返回400
- [ ] 参数值超出范围时返回400

### 6.2 数据不存在
- [ ] 查询不存在的消息ID返回404
- [ ] 查询不存在的用户返回404

### 6.3 并发冲突
- [ ] 同时删除同一条消息
- [ ] 同时标记已读同一条消息

---

## 七、数据库验证

### 7.1 数据一致性
```sql
-- 验证消息总数
SELECT COUNT(*) FROM messages;
-- 结果：______

-- 验证用户消息关联数
SELECT COUNT(*) FROM user_messages;
-- 结果：______

-- 验证软删除不影响数据库记录
SELECT COUNT(*) FROM user_messages WHERE is_deleted = 1;
-- 结果：______
```

### 7.2 索引使用
```sql
-- 验证查询使用索引
EXPLAIN SELECT * FROM user_messages 
WHERE user_id = 1 AND is_read = 0 AND is_deleted = 0;
-- 使用的索引：______
```

---

## 八、Bug列表

| Bug ID | 模块 | 严重程度 | Bug描述 | 复现步骤 | 状态 |
|--------|------|----------|---------|----------|------|
| 1      |      |          |         |          |      |
| 2      |      |          |         |          |      |
| 3      |      |          |         |          |      |

---

## 九、测试总结

### 9.1 测试覆盖率
- 后台管理API：____/18 个接口通过
- 前台用户API：____/10 个接口通过
- 模板管理API：____/4 个接口通过
- **总体通过率**：______%

### 9.2 功能完整性评估
- □ 所有核心功能正常
- □ 存在部分功能缺陷
- □ 存在严重功能缺陷

### 9.3 性能评估
- □ 性能满足要求
- □ 性能需要优化
- □ 性能存在严重问题

### 9.4 建议和改进
```
记录测试过程中发现的问题和改进建议...

1. 
2. 
3. 
```

### 9.5 上线建议
- □ 可以上线
- □ 修复Bug后上线
- □ 需要重大改进后上线

---

**测试人签名**：________________

**日期**：____年____月____日
